version: '3.8'

services:
  api:
    build: .
    container_name: desktop-stock-api
    ports:
      - "${API_PORT}:${API_PORT}"
    volumes:
      - ./stock.db:/app/stock.db
      - ./logs:/app/logs
    env_file:
      - .env
    restart: unless-stopped